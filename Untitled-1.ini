var name=prompt("Как вас зовут?");
console.log ("Привет, "+ name);
var likesCats=confirm("Тебе нравятся кошки?");
if (likesCats) {
    console.log ("Классная кошка!");
} else {
    console.log ("Что ж, не проблема!");
}
alert ("JavaScript это  здорово!");

выбрать случайное слово 
пока слово нугадано {
    показаит игкроку текущее состояние игры
    запросить у игрока вариант ответа 
    если игрок хочет выйти из игры {
        выйти из игры
    }
    иначе если вариант ответа - не одиночная буква {
        сообщить игроку, что он должен ввести букву
    } 
    иначе {
        если такая буква есть в слове {
            обновить состояние игры, подставив новую букву
        }
    }   
}
поздравить игрока с победой - слово угадано

["_", "_", "_", "_"]
["_", "ы", "_", "_"]
["р", "ы", "б", "а"]

var words = [
    "программа",
    "макака",
    "прекрасный",
    "оладушек"
];
var word = words[Math.flopor(Math.random() * words.length)];
var answerArray = [];
for (var i = 0; i < word.length; i++) {
answerArray[i] = "_";
}

var remainingLetters = word.length;

while (remainingLetters > 0) {
    // основной код
    // показываем остояние игры
    //запрашиваем вариант ответа
    // обновляем answerArray и remainingLetters для каждого
    // вхождения угаданной буквы
}

alert(answerArray.join(""));

var guess = prompt("Угадайте букву или нажмите Отменя для выхода из игры.");
if (guess === null) {
    break;
} else if (guess.length !== 1) {
    alert("пожалуйста, ввдите только одну букву.");
} else {
    // обновляем состояние игры
}

for (var j = 0; j < word.lenght; j++) {
if (word [j] === guess) {
    answerArray[j] = guess;
    remainingLetters--;
}
}
